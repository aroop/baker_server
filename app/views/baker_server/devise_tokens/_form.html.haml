= simple_form_for(@devise_token, :html => {:class => 'form-horizontal' }) do |f|
  = f.error_notification

  .form-inputs
    = f.input :token, input_html: { class: "span6" }, hint: ''
    = f.association :user, input_html: { class: "span6" }, hint: ''

  .form-actions
    = f.button :submit