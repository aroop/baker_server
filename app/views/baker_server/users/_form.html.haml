= simple_form_for(@user, :html => {:class => 'form-horizontal' }) do |f|
  = f.error_notification

  .form-inputs
    = f.input :email_address, input_html: { class: "span6" }, hint: ''

  .form-actions
    = f.button :submit